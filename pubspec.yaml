name: support_room_management
description: "A new Flutter project."
publish_to: "none"

version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
    
  web: ^1.0.0
  flutter_riverpod: ^2.5.1
  supabase_flutter: ^2.5.6
  intl: ^0.19.0
  go_router: ^14.2.0
  flutter_svg: ^2.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/google_logo.svg
    - assets/images/app_logo.png
